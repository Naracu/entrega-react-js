import { Box } from "@chakra-ui/react";
import { navBar } from "../Components/navBar";



const MainLayout = ({children}) => {
    return(
        <Box>
            {childrem}
        </Box>
    );
};